# 07: Limpeza rápida do repositório

**What to build:** Correções pontuais de configuração (limpeza do `.gitignore`, ajuste de dependências no `pyproject.toml` e metadados no GitHub) aplicadas e unificadas em um único commit de manutenção.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Remover a entrada `agy.exe` do `.gitignore`.
- [ ] Mover a dependência `pytest` para a seção de dev-dependencies no `pyproject.toml`.
- [ ] Preencher a descrição e as tags/topics do repositório no GitHub (ou documentar para o admin fazer).
